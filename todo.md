# TODO（UI 美化 + 造数据）

## 一、界面可美化项

### P0（优先做）
- [x] 统一视觉规范：定义全局色板、按钮样式、卡片阴影、圆角、间距（避免各页面风格不一致）。
- [x] 顶部导航高亮当前页面（Dashboard / 分析查询 / 成员管理 / 树形展示 / 祖先树）。
- [x] 表单交互增强：输入校验提示、提交 loading、成功/失败 toast 提示统一化。
- [x] 分析页 `analysis-page` 的结果区可视化：JSON 改为表格/统计卡，支持空结果态提示。

### P1（次优先）
- [ ] 树形页面优化：节点连线样式、父系/母系颜色区分、深层折叠展开。
- [ ] 成员管理页增强：行内编辑操作按钮布局优化，删除按钮二次确认弹窗。
- [ ] Dashboard 增加小图表（男女比例饼图、成员数量趋势占位）。
- [ ] 移动端适配：小屏下表格改卡片，按钮区改纵向堆叠。

### P2（体验优化）
- [ ] 深色模式（可选）与主题切换。
- [ ] 页面级 skeleton/loading 骨架屏。
- [ ] 空数据插画和引导文案（首次使用体验）。

---

## 二、造数据 TODO

### A. 基础数据脚本完善（`scripts/generate_family_csv.py`）
- [ ] 增加参数：每代性别比例、婚配概率、平均子女数分布（泊松/区间随机可选）。
- [ ] 增加参数：在世概率（控制 `death_year` 为空比例）。
- [ ] 增加姓名生成策略：支持中文姓氏池 + 名字池，避免 `Name10001` 过于机械。
- [ ] 增加关系异常保护：防止自环、重复 parent-child、跨族谱关联。

### B. 多规模数据集
- [ ] 小样本：1 个族谱 / 50 人（开发联调）。
- [ ] 中样本：3 个族谱 / 1,000 人（功能测试）。
- [ ] 大样本：10 个族谱 / 10,000+ 人（性能压测）。
- [ ] 每种样本输出固定随机种子，保证复现。

### C. 导入流程标准化
- [ ] 固化导入顺序文档：`member -> parent_child -> marriage`。
- [ ] 为 `sql/import_export.sql` 增加 Windows 路径示例与权限注意事项（`secure_file_priv`）。
- [ ] 补一个一键导入脚本（PowerShell）：自动检测文件存在并逐表导入。

### D. 数据质量校验
- [ ] 导入后校验 SQL：计数、孤儿记录、非法关系（father/mother 之外）检查。
- [ ] 校验年龄合理性：`birth_year <= death_year`（若 death_year 不为空）。
- [ ] 校验亲缘路径可达性抽样（随机 100 组成员）。

---

## 三、建议执行顺序
- [ ] 第 1 周：完成 UI P0 + 小样本数据稳定生成。
- [ ] 第 2 周：完成 UI P1 + 中样本导入与校验。
- [ ] 第 3 周：完成大样本压测 + UI P2 收尾。
